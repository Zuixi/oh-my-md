Use `inline code` for short snippets.

Use double backticks for content with one backtick: `` `with backtick` ``.

Wrap in more backticks for double backticks inside: ``` ``two backticks`` ```.

Code spans can contain **markdown** and *italic* literally.

End of `code` examples.
