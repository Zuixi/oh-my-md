Plain *italic* and **bold** text.

Nested **a *b* c** inside bold.

Also _underscore italic_ and __underscore bold__.

Triple ***bold italic*** together.

Strikethrough uses ~~double tilde~~ syntax.

Mix **bold with `code` inside** away.
