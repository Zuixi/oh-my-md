# Changelog

All notable changes to this project are documented here, generated from conventional commits.

## v0.0.1 - 2026-09-04

### Bug Fixes

- Resolve live-preview input issues and update .gitignore (807527a)
- Wrap iterate enter callback in block to satisfy boolean|void return type (1a854fe)
- Add katex dep so its CSS resolves from the desktop bundle (c960746)
- Enable tauri protocol-asset feature required by assetProtocol; commit lockfile (e032f78)
- Four M2 runtime root causes — StateField block decorations, replace-only atomic ranges, inclusive block boundary, click-to-edit widget (cfe00e8)
- Dblclick-to-edit, perf — remove pos from BlockWidget.eq, add Shiki debounce+cache, expand lang support to 63 langs (f9e0af1)
- Stabilize M2 — incremental preview, atomic save, image paste (8bc252d)
- Fix up/down arrow jumps and right-click paste bug; add setext headings folding (53f4569)
- Render CJK underscore emphasis, HRs, footnotes, and highlight (da02f76)
- Render <u> as underline in live preview (f41dc5b)
- Hide quote markers while typing on the same line (20934fc)
- Write PNG exports from createPDF so a file actually appears (e6f4cb4)
- Keep normalization actions focusable and announceable (938d860)
- Report a failing recovery write once per tab so a broken recovery directory cannot block editing (8865114)
- Harden file tree reveal and virtualized row height (491fbc9)
- Support autolinks and reference links (e1b3151)
- Serialize DiskSnapshot variant fields as camelCase so opened files keep their name (f6b80a3)
- Strip any Co-authored-by trailer in commit-msg hook (419f54d)
- Keep quote prefixes when continuing lists (e9d4bb9)
- Prevent find next from firing twice (3c67b97)
- Give list Enter precedence and keep quote on exit (bb57218)
- Hit-test footnote definition marks only (fe4bfe8)
- Normalize local markdown link paths (b5ce093)
- Clear footnote jump on document reset (557f20e)
- Keep table trailing newline and reject ragged tables (4ca1dbb)
- Keep table toolbar on the keyboard-focused cell (52ae39f)
- Replace the live table range after the widget moves (597fce4)
- Keep image picker on the starting document (ba0d4f4)
- Skip the image picker on untitled documents (2386926)
- Confine workspace mutations to authorized roots (96f911a)
- Create from empty tree and drop stale recovery on rename (0e1978d)
- Richly render nested math and honor image resolver (e4172ce)
- Skip find matching while the bar is closed (7b1bdb4)
- Link against _dispatch_main_q instead of the dispatch_get_main_queue macro (0edb4f2)
- Remove unused StoredLocale import workaround (7f724e8)
- InitLocale emits so post-mount callers re-render (92c3917)
- I18n command palette labels (3e3563a)
- Remove incomplete updater plugin that blocked dev boot (213dd81)
- Remove duplicate top-level Find submenu (34f4934)
- CrossLayerMenu regex for localized menu labels (0f60fa8)
- Persist settings and recovery state in app data instead of temp dir (b26ffb7)
- Move tree deletions to the OS Trash instead of permanent removal (855af8b)
- Stabilize incremental decoration tests under CPU load (b887b39)
- Guarantee full parse in makeState and bound large.md snapshot (60a5ac1)
- Flush pending edits before the tree-delete dirty check (463b8fd)
- Reapply render budget per active tab and clean safe-mode state (585b407)
- Hide mac-only export commands off macOS and neutralize reveal copy (2776d12)
- Scope desktop-only export copy to the mac desktop app (d73b8e4)
- Cover windows and linux fonts in editor stacks (7f23c9d)
- Make version sync script portable across sed variants (f81162c)
- Resolve diagnostics log dir and os info cross-platform (a984a28)
- Gate webkit contextmenu workaround off windows webview2 (2cbf228)
- Keep overwriting saves reliable on windows rename semantics (e1ba42e)
- Preserve io error kinds through replace_existing (f523a25)
- Format tooltip shortcut hints per platform (00dc8ea)
- Align zh view menu label across platforms (7d737ef)
- Guard RunEvent::Opened for macOS only and fix verify script (2dbc3f9)
- Windows-specific test failures in Rust tests (7821e56)
- Skip directory sync on Windows (NTFS already provides durability) (147525f)
- Serialize config-env tests and skip sync failure test on Windows (6cdcb4c)
- On Windows distinguish missing parent dir from present dir in sync_parent (e89d686)
- Bind Vite dev server to IPv4 127.0.0.1 to fix Windows EACCES (9f5f7da)
- Stop file-switch flicker and large-doc open stalls (644bcc5)
- Mirror Live/Source toggle into the status bar on keyboard shortcut (47d26c7)
- Last-file close, tree-name wrap, and fixed sidebar width (c329e21)
- Fold link titles in live preview so they stop leaking into rendered text (9649469)
- Open the file created from the tree so it becomes the active document (11b9622)
- Reveal in file manager was a no-op (wrong plugin invoke payload) (05392c7)
- Focus the editor after creating a file so typing starts immediately (88858f0)
- Close the create-then-delete race so a deleted file never keeps its tab (72b5386)
- Scope open invalidation to in-flight opens so re-created files still open (96e6b4f)
- Stop selection highlight extending past line ends and over empty lines (2da10ff)
- Size the Windows localized font-name buffer to include the NUL terminator (b6d6842)
- Keep readonly docs immutable across engine keymaps and renumber (6703131)
- Keep readonly docs immutable across widgets and rich paste (15c6c71)
- Keep active-tab windowing on restore and readonly docs immutable (d2a617b)
- Flush session state before quit so restore keeps the tabs left open (4916a4e)
- Sync dark mode to native scrollbars, title bar, and settings buttons (ee7ae19)
- Eliminate title bar white flash at startup by applying theme in Rust setup (895b203)
- Exclude line-start and cross-line atoms so pointer selection cannot cascade across lines (30a3351)
- Keep block widgets rendered with a covered overlay when a selection fully spans them (d98c9df)
- Only a collapsed caret reveals inline markdown marks, selections stay visual (dda62f9)
- Arrow keys step into rendered blocks line by line instead of skipping them (af0b793)
- Place caret at end of pasted content instead of before it (ff2f74c)
- Resolve cold-start file association and rename binary to omd (ef5e3ad)
- Align code-block click caret to rendered line (687e10c)
- Refresh block widget geometry after async render (db7b5c7)
- Resolve code clicks from current widget range (101e4aa)
- Measure code widgets before async highlighting (ef62336)
- Keep live-preview clicks on the line the pointer hits (9627877)
- Fill tight selection bars to the line box (baa5f18)
- Align multi-line selection at shared content leftSide (7b6b4d6)
- Anchor selection geometry at documentTop, not the content box (121923a)
- Align selection left edge with the text, not the content border box (f8f4bed)
- Stop the active-line decoration from tinting whole blocks blue (8170dc3)
- Keep soft-wrap selection on one visual line and table edits layout-stable (eb3755a)
- Use fixed-aspect BMPs so the Windows installer logo is not stretched (d13c385)
- Keep Live code blocks rendered on click like Typora (d84ec3b)
- Drop unused unregisterBlockWidget import blocking desktop build (0aec3ef)
- Tighten code block gutter and add downward language picker (85f8adc)
- Restore native source edit for live code blocks (b9dec44)
- Show code-block fence chrome by default (5fc7ce3)
- Keep WiX banner title strip clear of baked-in branding (2a7562a)
- Drive editor selection colors from the theme token (147469e)
- Scroll outline jumps to the target heading (e326e2a)
- Fold decorative marks regardless of caret position (901733f)
- Keep the code-block caret line gray under activeLine (9ac4426)
- Paint the sidebar resizer chrome to close the white seam between sidebars (84a6210)
- Size panel tool buttons from one shared token (7e71334)
- Open rendered links through system browser (8bad56d)
- Accept finite bounded cache sizes (5579793)
- Clear stale scale metadata on lazy retry (81fe874)
- Ignore materializer queue after destroy (e287ebe)
- Guard stale runtime callbacks (0d450aa)
- Make capped search selection deterministic (36f33ed)
- Clean test warning paths in save conflict region and harness (f8f5eb3)
- Re-render math popup preview by comparing src in widget eq (4d17f77)
- Resolve math popup write-back range from live syntax tree, not stale widget offsets (5210478)
- Keep math popup focused after click (fe342a0)
- Unmount mermaid widget when the caret enters the block (1a83381)
- Click a rendered code block to edit at the clicked line (aaed560)
- Reveal link-family syntax only when the caret enters its span (2fd599f)
- Keep tables rendered when clicking their non-cell surface (28fd77d)
- Preserve empty table edge cells (c73dcb7)
- Preserve one-sided table outer pipes (4731ecf)
- Update only the edited table cell (e27048e)
- Skip synthetic table cells on tab (1013a03)
- Isolate table edit focus per editor (571d21b)
- Preserve table source during row and column edits (dfa7756)
- Map widget delete-row to the 0-based data-row index (84c52c7)
- Complete range-based table toolbar edits (c910198)
- Support short table delimiter markers (44d640d)
- Allow mouse caret placement in math popup (e3aa02c)
- Apply selected font to code surfaces (eb38316)
- Drop stale restored workspace folders and stop repeat-listing toast spam (1c88b39)
- Require an open-intent modifier click to navigate links (7abfd38)
- Keep blank lines in verbatim blocks full height (e8e438c)
- Render the editing code chrome as its own block line (84f829e)
- Make dark-mode caret and selection visible via theme tokens (7b73a0d)
- Keep blank lines visible in rendered code blocks (838676b)
- Allow safe release continuation (3550a47)
- Share release continuation preflight (0075a8c)
- Align release config with manual publishing (4d68d3c)
- Install Linux SVG rendering dependency (0d1bf9b)
- Detect actual runtime bundle type for update capability (6583eca)
- Harden stable promotion against injection and preserve history (d864dbd)
- Recheck update readiness at install and harden CI contracts (edcfdc6)
- Use Tauri MSI bundle name on Windows (b48d677)
- Gate macOS export helpers by platform (1e098fd)
- Harden release signing and warning gates (67c902b)
- Isolate updater probe key source (a1ec035)
- Match Linux AppImage updater artifacts (4da9cc2)
### CI

- Add engine/desktop/rust/link workflow (ff0b678)
- Compile and link the rust app on linux (556264d)
- Compile and link the rust app on windows (d5fbf35)
- Run jobs only for changed domains and merge rust/link to halve OS runners (4cbcb25)
- Build complete draft releases (c8e25f2)
- Pin Windows release runner (292e779)
- Publish signed updater candidates (8388903)
- Control stable update promotion (5f2456d)
### Chores

- Merge GitHub initial LICENSE into main (6e7bf3a)
- Track pnpm-lock.yaml for reproducible installs (862c7f5)
- Sync pnpm-lock.yaml after adding @types/node (49bc7eb)
- Gitignore .pi/ agent artifacts (aa2f66c)
- Run tsc --noEmit before vitest to catch type-only bugs vitest misses (0f3a7de)
- Commit Cargo.lock for protocol-asset feature (8a85b61)
- Drop unused Range import caught by desktop tsc (df7ce04)
- Add pnpm verify workspace gate (1a17ac3)
- Add build.sh and test.sh entry points (14045e6)
- Add opencode permission gate for dangerous commands (cb270ef)
- Remove unused wordCount helper (4f30d1a)
- Merge daily-writing-gaps into m2-block-widgets (74852b9)
- Sync Cargo.lock for log and zip plugins (e66606e)
- Sync Cargo.lock for updater plugin (02eb1b0)
- I18n minor polish (4973dcf)
- Forbid cpu busy loops in guard and agent rules (0f2c1ba)
- Make CONFIG_ENV_LOCK test-only to avoid dead-code warning (4a480f1)
- Change Vite dev port from 1420 to 9420 to avoid common conflicts (d6e9179)
- Ignore AI tool state dirs (.mimosa/ .zcode/) (c28ebe6)
- Adopt generated app icon set (master app-icon.png + all platform sizes) (be8552e)
- Tune vscode workspace theme colors (27d6a2c)
- Bump actions/upload-artifact from 4.6.2 to 7.0.1 (0d4daa6)
- Bump dorny/paths-filter from 3.0.4 to 4.0.3 (6fb82e2)
- Bump actions/checkout from 4.4.0 to 7.0.1 (13c07be)
- Bump softprops/action-gh-release from 2.6.2 to 3.0.2 (73a7734)
- Merge main into table editing reliability (9bbb58f)
- Restore trailing EOF newlines in updater files (57474f4)
### Documentation

- Add oh-my-md design spec (Typora competitor) (70ee919)
- Fix spec filename date (7e0427e)
- M1 engine implementation plan (c723122)
- V0.1 (M1) manual QA checklist (7b3eb6f)
- Sync manual-qa with current implementation; clarify M1 table scope in spec (11e8d07)
- M2 block widgets implementation plan (0c4128a)
- M2 manual QA items + updated baselines (5edff85)
- Capture M2 incident lessons in known-gotchas; fix two outdated facts (a27fc98)
- Document safe list normalization workflow (3f7b325)
- Document conflict-safe guarded save (39ab0ec)
- Plan filetree layout performance (e9a5baf)
- Note stale rust target artifacts break the link (836da67)
- Add user settings and session restore design spec and plan (a74ddef)
- Complete settings and session restore implementation plan (d88e417)
- Record editor gap analysis and writing specs (0f7f70f)
- Add Apache-2.0 license and README (6815ddd)
- Domain guides, release spec and plans (fa8b36e)
- Add i18n dual-locale runtime-switch design spec (fad8e7a)
- Add i18n dual-locale implementation plan (0cb13a5)
- I18n manual QA and desktop AGENTS command entry (c09c759)
- Note i18n store ownership and engine host-option in AGENTS (82bebf1)
- I18n fixes implementation plan (bd5dd74)
- Record apple-account blocker and refresh stale status (81f9abb)
- Fill release instructions and changelog for release blockers round (7be37eb)
- Write perf and ai boundary specs and sync roadmap (5780a28)
- Plan release unblock and large-document performance (6689ec1)
- Release workflow docs and smoke checklist (451f8dd)
- Sync 13b status after updater secret upload (e48e421)
- Reconcile 13b status with delivered B4 (617948e)
- Perf smoke script, advisory CI bench, and numbers (9eeabec)
- Mark 05 large-doc performance plan complete (b263fe3)
- Spec 05a huge-doc typing diagnosis and design (677cfa9)
- 05a huge-doc typing implementation plan (6380d7a)
- Sync 05a manual qa and plan completion (5b1a9ce)
- Add cross-platform spec and implementation plan (47ab1ea)
- Document platform conventions and p0 regression checks (06577df)
- Add linux manual qa checklist (c9eeda0)
- Add windows manual qa checklist (4e1c301)
- Flag three os ci matrix as unobserved (f9c02cd)
- Add high-value polish implementation plan (4 features) (ffeaabe)
- Pin reveal rejection contract and multiline link fold caveat (d02da0a)
- Spec the toast notification system (ef4b606)
- Cover the conflict choose-another-path save toast trigger (0afe3e0)
- Add the 2026-08-19 industry self-assessment snapshot (33ef780)
- Add the system font picker implementation plan (eca340d)
- Cover the system font picker in QA notes and gotchas (b1d8032)
- Tidy the font gotcha cross-reference and trailing whitespace (219ec2a)
- Add the large-file live render and fast-switch implementation plan (ef5732a)
- Record the progressive-rendering invariants, readOnly trap, and bench/QA coverage of the perf wave (c3c7016)
- Rewrite README for open-source launch (EN + ZH) and add CONTRIBUTING.md (cf5c333)
- Update READMEs and CONTRIBUTING for cross-platform support (221d3ba)
- Update repo references from open-md to oh-my-md (d0c5601)
- Record selection-is-visual invariants, atomic Rule 3, and block-entry QA items (1ee3f65)
- Define optimization workstreams (e1ef787)
- Plan runtime optimization workstreams (a844495)
- Record optimization baseline (4a249c7)
- Preserve baseline evidence (7893209)
- Add rust baseline details (ba39829)
- Record editor status and folder search QA (e64ca8e)
- Record optimization results (a45e131)
- Refresh final optimization evidence (4cd6c04)
- Add math popup editor design and plan (3d2a770)
- Correct math popup write-back range clause in spec (af1096b)
- Record span-level reveal and click-to-edit interaction invariants (15cb8c4)
- Pin the tabler icon version and name the source-toggle button accurately (24807de)
- Document reliable table editing invariants (5fac9ab)
- Record table editing reliability plan (dab102f)
- Define editor interaction and layout reliability (2c1a128)
- Plan editor interaction and layout reliability (c8669df)
- Add editor interaction reliability QA (288a3db)
- Clean table reliability spec whitespace (4988f1f)
- Record the CM6 math popup contract, Mod-key gotcha, and QA steps (2e73690)
- Record the persistent code chrome and nested-language highlighting invariants (8a97bfd)
- Require concurrent sessions to work in dedicated worktrees (93936ec)
- Split known-gotchas into a one-line index plus domain files (5a1187c)
- Define oh-my-md release skill (17f8460)
- Add guarded release skill (94c9c73)
- Prepare v0.0.1 release guidance (da7a451)
- Record release readiness implementation plan (5062ace)
- Define signed automatic updates (d609fda)
- Settle automatic update platform rules (1917105)
- Plan stable automatic updates (83680f4)
- Document stable automatic updates (696dfc4)
### Features

- Monorepo scaffold + engine package skeleton (fcfd4ea)
- Lezer markdown language with GFM (footnotes deferred to custom extension) (0879bee)
- Decoration pipeline core + heading folding (824ae24)
- Fold bold/italic/strikethrough/inline-code/link syntax marks (338266a)
- List/task-checkbox/blockquote/horizontal-rule decorations (d46b85d)
- Live/source mode switch via compartment + Cmd-E toggle (7338e04)
- Tauri 2 + React shell mounting CM6 editor with live preview (96f7e8a)
- Read_file/write_file commands + dialog plugin (89bcbb9)
- Custom Footnotes Lezer extension + superscript/label-fold decorations (c88ccaa)
- List nesting/indent styles, fenced-code block styling, footnote continuation lines (dd4ce48)
- BlockWidget lifecycle base + pipeline subtree-skip/conflict-filter/atomic-ranges (e3139f1)
- TableWidget with align support + source edit toggle (2bb8e5e)
- CodeWidget with lazy-loaded shiki highlighting + source edit fallback (09559e0)
- Lezer math extension — $$ block + $ inline (0ddde62)
- KaTeX rendering for $$ block and $ inline math (lazy-loaded) (e92dbaa)
- MermaidWidget — lazy mermaid, 500ms debounce, strict security (7e65630)
- Write_image command (base64 decode + mkdir -p) (5a23319)
- Paste screenshot → write to assets/ → insert relative image link (540f2ae)
- ImageWidget with host-injected src resolver + asset protocol (9346b44)
- Keep ordered-list source in sync with preview numbers (0f501f0)
- Render nested quotes, entities, and gemoji in live preview (2afa521)
- Add the product shell so folders stay expandable from a File menu (9d6cb5e)
- Export PDF and images through native WKWebView (3f351e2)
- Track pending ordered-list normalization so rewrites stay reversible (743fe53)
- Keep ordered-list normalization revertible across parse-progress batches (e006e97)
- Project normalization state per tab so async work cannot cross documents (f2cb302)
- Let users review ordered-list normalization without a modal (78e8f07)
- Show pending list normalization for the tab it belongs to (680b171)
- Confirm automatic list normalization (ec53e9f)
- Read documents with content fingerprints (aa4cfbf)
- Classify document version conflicts (ed51018)
- Serialize saves per resolved path (ebdac05)
- Guard document saves with double version compare (340212d)
- Preserve permissions and user metadata on save (e5b2c91)
- Model per-tab save lifecycle and divergence (6aba2d2)
- Add typed document IPC contracts for guarded save (c8837fb)
- Add document save coordinator pure functions (9b661ea)
- Add read-only unified diff for conflict compare (6c2f4ba)
- Add accessible save conflict UI (8806224)
- Save documents through guarded versioned writes (dbb4ce5)
- Resolve save conflicts without silent overwrite (d21f35e)
- Arrange shell into sticky three-column layout (eaa4ebc)
- Redesign topbar to modern macOS single-row tab bar (5ab8e6c)
- Add sticky sidebar collapse toggle and independent search bar (00bb25d)
- Improve line wrapping, outline popover, and markdown table rendering (afe8234)
- Render full markdown inside table cells (818dc7f)
- Add settings and session state persistence commands (e5350ce)
- Add settings and session restore models and services (8cafcd6)
- Add settings and preferences modal component (471ee39)
- Integrate user preferences and workspace session restore in desktop app (327dead)
- Add visible Settings button in TopBar and native macOS app menu (c0b0908)
- Count CJK-aware document stats (ab43d55)
- Continue and indent lists from the keyboard (8d743a0)
- Add literal find and replace helpers (1b6783d)
- Add document find, spellcheck, and CJK status counts (78b4eb0)
- Resolve footnote and local markdown targets (e3a4900)
- Open local markdown links and jump footnotes (6d50869)
- Transform markdown table cells and axes (9d77e44)
- Edit live-preview tables in place (746002f)
- Insert images by drop and file picker (350c7d2)
- Add workspace create rename and delete commands (7a97fc7)
- Manage files from the sidebar menu (f2a105b)
- Export HTML with math code and diagrams (ee9ccd2)
- Wait for export render before PDF capture (b77d8d8)
- Single-source version with sync script and drift test (fa031a1)
- Generate CHANGELOG from conventional commits (3f646ff)
- Structured logging via tauri-plugin-log (46e46ca)
- Export redacted diagnostics bundle (937a78d)
- Update check wrapper via tauri-plugin-updater (ba9e0f3)
- Native menu with format/view/window and checkbox state (c8c440f)
- Desktop shell polish (folder search offsets, workspace ops, settings) (51a8283)
- Inject imageBrokenLabel via Facet for i18n (388026b)
- Add i18n external store with useSyncExternalStore (d29a2f0)
- Add locale field to UserSettings (6eb0cb6)
- Localize native menu and add set_menu_locale command (2558591)
- Wire initLocale/setLocale and setMenuLocale service (de8b738)
- I18n FindReplaceBar as migration pattern (8dfcfcb)
- I18n sidebar topbar status outline components (c46f976)
- I18n conflict and normalization banners (3927b46)
- I18n App outline prompts and save status (f08c8d5)
- I18n SettingsModal and add language picker (c2f6c92)
- Pass imageBrokenLabel into editorExtensions (80a03c4)
- I18n confirm dialogs export error and session label (e833069)
- Localize format view window find edit menu items (6416aab)
- Key-based conflict banner and action labels (360ec42)
- Key-based durability and normalization status (bd277a1)
- Localize user-facing error prefixes (961603b)
- Localize predefined edit and app menu items (0d8b404)
- Open markdown files launched from finder and open-with (cdbce33)
- Wire the updater plugin with startup and manual update checks (dd6f2db)
- Render code blocks with a matching dark theme (08ab9ed)
- Add regex and whole-word matching to find and replace (20d198f)
- Add quick open file picker with cmd+p (54223f0)
- Open markdown files dragged onto the window (c6b997b)
- Watch files natively and demote polling to a fallback (15ac126)
- Convert rich clipboard html to markdown on paste (e0883cc)
- Fold yaml front matter into an editable chip (4f798e7)
- Keep local version snapshots on every save (f297341)
- Surface export diagnostics in menu and palette (b97cc55)
- Add explicit setLivePreview engine API (7ebb584)
- Shared large-document threshold constants (c7d9f37)
- Safe mode for very large documents (aa68fdd)
- Block render budget for safe mode (864a5d2)
- Apply render budget in safe mode (c6d5f73)
- Add platform detection module for cross-platform support (07bab18)
- In-app menu for windows and linux parity with native menubar (89e866a)
- Add arrow key navigation to the app menu (3407770)
- Add Windows PowerShell scripts for cross-platform verify (d062a59)
- Replace the windows hamburger panel with a native-style in-app menubar (c99571d)
- Large document open performance (Spec 05b) (be2a573)
- Subscript/superscript inline syntax (45d0d6d)
- Add paste-as-plain-text command (5bca8da)
- Self-contained export HTML with base typography and custom CSS (be680c0)
- Warn when exporting PDF/PNG with remote images (fb9425b)
- Equal-width tabs so close stays under the pointer (4cd82ad)
- Toast notifications for errors and file operation feedback (b9c4366)
- Enumerate installed font families for the settings picker (2e8f74a)
- Expose the system font list service and family helpers (b383cac)
- Searchable system font picker in the settings panel (34b33d4)
- Seed live decorations progressively instead of full-document builds (af327fd)
- Drive pending live decoration build in idle slices (a691259)
- Bound safe-mode live decorations to the viewport window (05029f8)
- Open large and huge docs in live mode now that decoration builds are progressive (5455b17)
- Polish Windows installer branding with sidebar panel and zh copy (9d976ce)
- Typora-like code block chrome with hover toolbar and line numbers (b54d2a6)
- Complete a typed fence into a closed block on Enter (0dbb47e)
- Shrink non-caret blank lines to half height (e9b2fbe)
- Fold list mark in task list items (b9571e4)
- Highlight math widgets on hover (5c6b11d)
- Identity-stable math widget with in-place preview (d9ab4f8)
- Edit math blocks in a popup with live preview (4f01b8d)
- Vendor open-source svg icons and swap the pencil for a code glyph (09e7c31)
- Complete keyboard table editing flow (dd47451)
- Improve editor scrolling and readable width (d416ab9)
- Replace the math popup textarea with a stex-highlighted CodeMirror (c7ae1e6)
- Polish the code block chrome — spacing, hover copy, radius, thin scrollbar (1f7d32d)
- Keep code chrome mounted and styled while editing the block (607117c)
- Highlight code natively in the editing state via nested languages (d2e6bc7)
- Complete fences on Enter anywhere in the document (d168e95)
- Add real-browser E2E layout invariants (Playwright) (4ba470c)
- Replace updater with release download notice (a5dd330)
- Classify update restart blockers (20371e2)
- Gate update restart on safe session flush (a9c0f43)
- Restore signed Tauri updater integration (2471d61)
- Coordinate user-controlled application updates (e72c702)
- Add safe automatic update interaction (d37da96)
### Performance

- Keep file tree and outline off the keystroke path (7457541)
- Guard and dedupe file tree polling (4a952f2)
- Virtualize large file tree rows (5b082f0)
- Reveal opened files in the file tree (b11480c)
- Move folder scans off the main thread (4db1fc1)
- Remove specific agent authorized features for genera agent (e229a0e)
- Count stats in one pass and memoize status bar counts (20ae9f8)
- Await only async nodes and cache math in rich export (eeb064f)
- Keep full-document scans off the per-keystroke path (20e9832)
- Debounce recovery writes and dedupe unchanged content (52c7716)
- Pull doc content on materialize cadence instead of per keystroke (3b717da)
- Bound safe-mode ordered renumber scans to viewport and edit ranges (4809804)
- Cache per-tab outlines so tab switches skip the full syntax-tree walk (37dd3e8)
- Keep the post-save dirty check O(1) and gate the find count scan for over-scale docs (0cde191)
- Probe document versions stat-first so background polls skip unchanged 50MB reads (13debd9)
- Assemble streamed open chunks into Text so large opens skip the full-string split (7b14257)
- Add bounded engine cache (e4d338b)
- Bound code highlight cache (caf4927)
- Lazy load shiki highlighter (1f09442)
- Cache table widget equality keys (1e5e0c0)
- Cache table widget equality keys (d3f21b8)
- Benchmark fragmented live build ranges (1cb6e7e)
- Isolate editor status subscriptions (a2ccc06)
- Publish lightweight editor status (0de147e)
- Stop rerendering app on every edit (bf7b8fb)
- Avoid empty search rerenders (ba86177)
- Reserve workspace search slots atomically (1481b4c)
- Batch parallel workspace search hits (0422170)
- Measure workspace search throughput (940889d)
### Refactor

- Apply ponytail-review — drop unused highlight dep, dedupe foldLineMark, simplify markTo (8529a78)
- Stamp editor updates with tab and document identity so a late callback cannot write into another document (be832bf)
- Bind session path, baseline, and version (8d6a04d)
- Share shiki language loaders between widget and export (56dd3f7)
- Name parser char codes and single-source engine shortcuts (b5a8a12)
- Address 05a review nits (sync markers, comments, test hygiene) (9619145)
- Drop dead recovery flush api and dead re-export (306c882)
- Derive shortcut labels from bindings per platform (83d6d70)
- Isolate document materialization (c340596)
- Isolate workspace search state (f509413)
- Centralize document scale policy (2389476)
- Unify document scale evaluation (2815262)
- Single-source editor status equality (4200722)
- Isolate markdown file matching (640631b)
- Derive table editing data from lezer (057ffb8)
- Remove legacy table widget paths (bd589c8)
### Testing

- Verify GFM table parse (widget deferred to M2) (79c8587)
- Fixture snapshot regression harness for core markdown structures (2938eed)
- Cargo tests for read_file/write_file (roundtrip + missing-file error) (e2b3e5f)
- Cover width-changing ordered-list markers so reject cannot half-revert (d1a6ee7)
- Isolate the two-batch merge case so it counts what its name claims (5808f64)
- Cover the legacy editor callback and make the ordered-list test double fail loudly instead of diverging from the engine (c04c545)
- Cover stale normalization save identity gaps (c25e990)
- Advisory large-document benchmarks (74d845b)
- Steady-state bench caliber and full-tree guard (263d9ff)
- Hover the outline toggle button in the preview popover test (8499de1)
- Hold the env lock in recovery_roundtrip to stop parallel races (c353540)
- Pin the empty-visibleRanges cursor fallback instead of the happy path (d8e1ca2)
- Bench the live-toggle cliff, windowed typing, and live open paths (c590de2)
- Pin docTexts stash chains, PATHOLOGICAL state parity, and fix review-ledger comments (d9a38dc)
- Close engine final review findings (89e1d60)
- Guard editor status identities (3538d40)
- Harden editor interaction reliability coverage (848f2f9)
- Remove unrelated parsing cost from chunk slicing (d72b648)
- Pin the README credits paragraph as a link-reveal regression fixture (4696b9a)
- Allow future synced release versions (fb817ca)
