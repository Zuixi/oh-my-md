{
  "name": "oh-my-md",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @omd/desktop tauri dev",
    "test": "pnpm --filter @omd/engine test",
    "verify": "pnpm test && pnpm --filter @omd/desktop test && pnpm --filter @omd/desktop build && cargo build --manifest-path apps/desktop/src-tauri/Cargo.toml --no-default-features && cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml",
    "prepare": "git config core.hooksPath .githooks"
  }
}